# Finlandtrip 작업 규칙

이 저장소를 수정하는 모든 GPT/에이전트는 아래 규칙을 우선 적용한다.

## 1. 기준 템플릿
- 모든 DAY 상세 페이지는 `day1.html`의 구조와 디자인을 마스터 템플릿으로 사용한다.
- DAY2, DAY3, 이후 DAY4+도 동일한 정보 밀도와 구성으로 만든다.
- 기존 DAY 페이지를 수정할 때도 가능하면 `day1.html`의 레이아웃, 카드 스타일, 타임라인 구조, 비용 요약 형식을 유지한다.

## 2. 모든 페이지의 필수 네비게이션
- **모든 HTML 페이지에는 반드시 첫 페이지인 `index.html`로 돌아가는 링크/버튼이 있어야 한다.**
- 권장 문구: `← 전체 일정으로 돌아가기` 또는 `🏠 전체 일정`.
- DAY 페이지에는 가능하면 이전 DAY / 다음 DAY 링크도 함께 둔다.
- 액티비티, 비교, 가이드 등 DAY가 아닌 보조 페이지에도 `index.html` 복귀 링크는 필수다.
- 새 페이지를 추가할 때 `index.html`에서도 해당 페이지로 이동할 수 있는 링크를 추가한다.

## 3. DAY 상세 페이지 필수 구성
각 DAY 페이지에는 아래 요소를 기본적으로 포함한다.
1. 상단 히어로 영역: DAY 번호, 도시/지역, 날짜, 하루 핵심 동선
2. `오늘 전체 동선` 섹션
3. 전체 또는 구간별 Google Maps 링크
4. `시간별 상세 동선` 타임라인
5. 각 일정 카드에 가능한 범위에서 이동수단, 예상 소요시간, 예상비용 표시
6. 식당이 포함되면 대표 메뉴, 예상 가격, 예약 필요 여부 표시
7. 액티비티가 포함되면 운영시간, 소요시간, 예약 필요 여부, 예상 가격 표시
8. 하루 예상비용 요약
9. 예약/주의사항/확인 필요 항목
10. `index.html`로 돌아가는 버튼

## 4. 정보 정확성
- 확정되지 않은 열차/버스/항공편 시간, 2026년 실제 운임, 운영시간은 임의로 만들지 않는다.
- 미확정 정보는 `확인 필요`, `예약 확정 후 업데이트`, `방문 전 재확인` 등으로 표시한다.
- 최신성이 필요한 교통, 운영시간, 가격은 가능하면 공식 소스를 우선 확인한다.
- 이전 시즌 가격을 사용할 경우 반드시 `참고 가격`임을 명시한다.

## 5. 일정 설계 원칙
- 실제 이동시간과 겨울철 변수까지 고려해 일정 사이에 버퍼를 둔다.
- 지나치게 많은 일정을 넣지 않는다.
- 식사, 이동, 짐 보관/찾기, 체크인/체크아웃 시간을 빠뜨리지 않는다.
- 오로라는 보장된 일정처럼 표현하지 않고 날씨/구름/활동에 따라 달라지는 조건부 일정으로 작성한다.

## 6. 파일명과 링크
- 메인 페이지: `index.html`
- DAY 페이지: `day1.html`, `day2.html`, `day3.html` ...
- 모든 내부 링크는 같은 저장소 루트를 기준으로 상대경로를 사용한다.
- 기존 파일명을 임의로 변경하지 않는다. 변경이 필요하면 모든 연결 링크를 동시에 수정한다.

## 7. 메인 페이지 업데이트
- DAY 페이지가 새로 생성되거나 핵심 일정이 변경되면 `index.html`의 해당 DAY 카드/요약도 함께 업데이트한다.
- 메인 페이지의 큰 여행 흐름과 개별 DAY 페이지 내용이 서로 모순되지 않게 유지한다.

## 8. 현재 여행 구조
- 여행 기간: 2026년 11월 핀란드 여행
- 주요 흐름: Helsinki → Rovaniemi → Levi 또는 Saariselkä → Helsinki
- DAY1은 `day1.html`이 현재 상세 페이지의 기준본이다.
- 이후 DAY는 사용자가 확정한 일정만 반영하고, 확정되지 않은 선택지는 임의로 확정하지 않는다.

## 9. 수정 전 체크
작업 시작 전 최소한 아래 파일을 읽고 최신 상태를 확인한다.
- `AGENTS.md`
- `index.html`
- 수정 대상 DAY 파일
- 디자인/구성 기준이 필요하면 `day1.html`

## 10. 수정 후 체크
- 모든 새/수정 페이지에서 `index.html` 복귀 링크가 실제로 연결되는지 확인한다.
- DAY 간 링크가 깨지지 않았는지 확인한다.
- `index.html` 요약과 상세 DAY 페이지가 일치하는지 확인한다.
- 미확정 정보를 확정 사실처럼 적지 않았는지 확인한다.
